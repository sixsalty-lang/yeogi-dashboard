# 여기어때 국내여행 인사이트 대시보드 - 설치 목록 (v2)
# v2 개선은 전부 기존 패키지/파이썬 내장 기능으로 구현되어
# v1과 설치 목록이 동일합니다. 새로 설치할 것은 없습니다.

streamlit>=1.30
pandas>=2.0
numpy>=1.24
plotly>=5.18
requests>=2.31

# prophet은 선택사항입니다. 없으면 앱이 자동으로 계절성 그래프로 작동합니다.
# 통계 예측 모델(설날·추석 연휴 효과 반영)을 쓰려면 아래 줄의 # 을 지우고 설치하세요.
# prophet>=1.1
